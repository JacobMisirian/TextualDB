namespace TextualDB.CommandLine.Lexer
{
    public enum TokenType
    {
        Conditional,
        Comma,
        Comparison,
        Identifier,
        Number,
        Semicolon,
        String,
    }
}
